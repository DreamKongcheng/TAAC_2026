name: CI

on:
  pull_request:
    paths:
      - ".github/workflows/ci.yml"
      - "config/**"
      - "src/**"
      - "tests/**"
      - "pyproject.toml"
      - "uv.lock"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/ci.yml"
      - "config/**"
      - "src/**"
      - "tests/**"
      - "pyproject.toml"
      - "uv.lock"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: CPU Unit & Integration
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true

      - name: Sync project environment
        run: uv sync --locked

      - name: Run torch lint rules
        run: uv run python scripts/lint_torch.py

      - name: Run unit tests with coverage
        run: uv run --with coverage coverage run --data-file=.coverage.cpu -m pytest -m unit -v

      - name: Summarize CPU test routing
        run: |
          uv run python - <<'PY'
          import os
          from tests import conftest

          with open(os.environ["GITHUB_STEP_SUMMARY"], "a", encoding="utf-8") as handle:
              handle.write("## CPU test routing\n\n")
              handle.write(f"- executed on ubuntu-latest: unit ({len(conftest.UNIT_TEST_FILES)} files)\\n")
              handle.write(f"- delegated to GPU runner: integration ({len(conftest.INTEGRATION_TEST_FILES)} files), gpu ({len(conftest.GPU_TEST_FILES)} files)\\n")
              handle.write("- reason: the repository pins CUDA-linked TorchRec/fbgemm wheels, so stack-level imports require libcuda.so.1 even for CPU assertions.\\n")
          PY

      - name: Upload CPU coverage data
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-cpu-data
          path: .coverage.cpu

  coverage:
    name: Coverage Report
    runs-on: ubuntu-latest
    needs:
      - test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Set up uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true

      - name: Download CPU coverage data
        uses: actions/download-artifact@v5
        with:
          name: coverage-cpu-data
          path: .coverage-artifacts/cpu

      - name: Restore coverage data
        run: |
          cp .coverage-artifacts/cpu/.coverage.cpu .coverage

      - name: Summarize coverage scope
        run: |
          {
            echo "## Coverage scope"
            echo
            echo "- enforced from the fast CPU CI workflow"
            echo "- GPU integration checks are available in .github/workflows/gpu-tests.yml"
            echo "- performance benchmarks are available in .github/workflows/performance-benchmarks.yml"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Enforce coverage gate
        run: uv run --with coverage coverage report

      - name: Export coverage XML
        if: always()
        run: uv run --with coverage coverage xml -o coverage.xml

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-xml
          path: coverage.xml
